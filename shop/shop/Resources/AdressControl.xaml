<UserControl x:Class="shop.Resources.AdressControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                          xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:shop.Resources" xmlns:viewmodels="clr-namespace:shop.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:OrderViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="110" d:DesignWidth="250">
    <Grid Background="#DCCBFF">
        <StackPanel Margin="15">
            <TextBlock Text="{DynamicResource AddAdress}" Style="{DynamicResource TextBlockStyle}"/>
            <TextBox Text="{Binding Address}" materialDesign:HintAssist.Hint="{DynamicResource Adress}"
         materialDesign:HintAssist.IsFloating="True" FontSize="12" FontFamily="Comic Sans MS"/>
        </StackPanel>
        
    </Grid>
</UserControl>
